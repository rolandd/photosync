fn main() {
    println!("Checking nom-exif API...");
    // This is a dummy file just to trigger compilation and see if I can find signatures
    // or I can just try to use MediaSource::seekable which is common in these libs
}
